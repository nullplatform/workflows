{
  "name": "nullplatform-workflows",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Production-grade workflow suites for the nullplatform workflow engine \u2014 executable documentation: clone, npm install, npx vitest.",
  "scripts": {
    "test": "vitest run",
    "validate": "find . -name '*.yaml' -not -path './node_modules/*' -not -path '*/checklists/*' | xargs np-workflow validate"
  },
  "devDependencies": {
    "@nullplatform/workflow-kit": "^0.1.0",
    "vitest": "^2.1.9"
  },
  "license": "Apache-2.0"
}
